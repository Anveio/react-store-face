import * as React from 'react';
import { Layout, Card } from '@shopify/polaris';

export default () => {
  return (
    <Layout.AnnotatedSection title="Inventory">
      <Card sectioned>Inventory</Card>
    </Layout.AnnotatedSection>
  );
};
